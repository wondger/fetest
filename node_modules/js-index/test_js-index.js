var jsIndex = require("./js-index");

var jsfile_path = "/Users/o/node/NodeJS/jslibsearch/source/underscore-1.3.3.js";
var js_global = "_";

/*
fs.readFile(jsfile_path, "utf8", function(err, data) {
	if (err) {
		console.error("Could not open file: %s", err);
		process.exit(1);
	}
	
	var tobeStored = jsIndex.libIndex(data, jsfile_path, js_global);
	debugger;
	console.log(tobeStored);
});
*/

jsIndex.libIndex(jsfile_path, js_global, function(err, data) {
	if (err) {
		console.error("Error using js-index: %s", err);
		process.exit(1);
	}

	var tobeStored = data;
	debugger;
	console.log(tobeStored);
});
